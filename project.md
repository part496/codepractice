# 项目实践与薄弱点记录

> 最近更新：2026-09-16
> 本文件记录项目实践中有证据支持的知识、思路和综合应用薄弱点，作为后续项目复习依据。算法刷题另见 [LeetCode-Progress.md](LeetCode-Progress.md)。

## 使用规则

### 记录与更新规则

- 只记录值得复习的理解问题：不会处理、概念混淆、不能解释执行过程，或难以把已学知识组合起来使用。
- 不把一次漏写符号、拼写疏漏、发现后忘记修改等小问题上升为能力短板；不能仅凭一处错误推断“边界意识差”“基础不牢”。
- 已能解释并自行发现的小疏漏不单独记录。本次用时判断漏写等号、编号多一且已自行测试发现的问题，不列入薄弱点。
- 区分“代码已修正”和“已经稳定掌握”。提示后改对，记录修正事实；只有后续脱离提示解释或迁移使用得到验证，才升级掌握状态。
- 同一问题后续复习时更新原条目，保留从首次暴露到修正、独立应用的变化，不重复堆积记录。
- 按实际完成的阶段评估。后续阶段尚未实现的功能，不算当前阶段的薄弱点。
- 不把命名风格、格式偏好、最佳实践建议或未经验证的潜在风险自动写成学习短板。
- 日期、独立程度或具体问题不清楚时，写“待补充”“待确认”“暂无详细记录”，不补造经历。

### 后续协助方式

- 先核对当前项目阶段和本文件中的掌握状态，再根据最新代码与用户说明评估。
- 优先按“指出问题 → 说明问题类别 → 给小提示 → 留给本人修改”的方式帮助。
- 未明确要求时，不直接给完整实现；能继续完成的设计，不为了追求最佳实践强迫重构。
- 复习嵌入后续项目功能即可，不为了复习不断追加新的核心需求。

## 当前项目与范围

| 项目 | 本次记录阶段 | 已验证进度 | 整体完成状态 |
|---|---|---|---|
| Java 学习任务清单管理器 | 七项核心功能与联合验收 | 最新代码重新编译，18 项核心流程检查与 11 项查询检查全部通过 | 核心功能完成；2026-09-16 按本人决定收尾 |

- 审查依据：2026-09-12 至 2026-09-16 的讨论，以及本地 `App.java`、`Task.java`、`TaskService.java`、`test.java` 的读取和实际运行。
- 完成范围：添加、删除、修改、展示列表、名称 / 类别 / 状态组合查询、完成状态双向修改、任务统计。
- 本人于 2026-09-16 明确决定：“剩下的我不做了，核心功能完成了就行了。”不再把简短项目说明、原文字面差异对齐或可选扩展列为本项目待办，按本次确认的完成范围结束项目。
- 原计划预计 6～8 小时，实际总投入时长未统计，不据此判断是否超时。
- 项目主体及问题修正由本人编写。助手提供审查、解释、小提示和独立测试；未代写或修改项目源码。组合查询在提示后由本人选择并实现“复制列表、逐轮筛选”的方案。
- 原约定知识范围截止 Lambda；最终实现使用增强 `for` 和 `Iterator`。另保留“课程任务”的类别名称，以及无效输入后重试的交互方式。这些与最初文字或知识范围有差异，核心验收通过不等于逐字对齐；不将这些差异自动列为能力短板，也不再因此追加收尾任务。
- 其他项目暂不补录未经本次整理核实的能力评价。

## 当前总体情况

- 已经完成一次包含七项核心功能的控制台、内存存储 Java 项目，能把程序入口、菜单、任务对象和集合管理组织起来，并通过本次核心流程与查询检查。
- 本次最终验收共 29 项：18 项核心流程、11 项查询检查全部通过。该结论限于实际检查场景，不等于证明任意输入下均无错误。
- 核心检查包括添加校验、空列表、同名任务按编号修改、删除及取消、删除后编号不复用、状态双向修改、非法输入重试、菜单入口，以及修改 / 删除 / 状态变化后的统计。
- 查询检查包括单条件与多条件同时筛选、三个条件与不同输入顺序、关键词包含匹配与大小写、首尾空白 / 空串 / 全空格、无匹配提示、非法选项重试，以及查询不改变原任务和统计。
- 固定统计数据验证通过：总数 3、已完成 1、未完成 2、总预计用时 100 分钟、未完成预计用时 80 分钟；空列表及删除最后一条后的统计均为 0。
- P001～P003 保留历史理解问题与修正记录；本轮补充 P004 组合查询、P005 遍历时删除、P006 编号分配规则。代码修正并通过当前场景，不直接升级为“已稳定掌握”。
- 本人说明菜单接入、状态恢复、部分失败分支的 `return`、空列表判断及 `trim()` 等属于已考虑过或已用过但忘记补写的地方。保留修正事实，不单独新增薄弱点，也不据此推断整体基础或边界意识差。

## 项目薄弱点明细

### P001．控制台输入：读取、处理、校验与转换的组合

- 日期：2026-09-12；修正验证：2026-09-13
- 项目 / 阶段：学习任务清单管理器 / 第一天
- 专题：Scanner、String、输入校验、数值转换
- 独立情况：基础添加流程自行完成；本项处理思路经提示后自行实现。
- 主要问题：最初用 `next()` 读取任务名称，含空格名称会被拆开，剩余文字进入后续类别读取；数字位置直接使用 `nextInt()`，遇到字母会中断。本人明确表示不会修改这两类问题，说明当时尚不能独立组合“整行读取 → 必要的文字处理 → 格式检查 → 数值转换 → 业务范围检查”。名称处理还需要区分空字符串与全空格输入。
- 修改与验证：改用整行读取；名称先去掉首尾空白，再检查长度并保存；类别与用时先检查格式，再转换。复测中，名称中间空格保留、首尾空格去除；空名称与全空格名称被拒绝；类别及用时输入字母后可以重新填写并正常完成添加。
- 掌握情况：已按提示完成并通过当前场景验证；后续独立组合这些步骤的能力待验证。
- 2026-09-16 复测补充：添加校验及修改编号非法输入后的重试通过；查询关键词去除首尾空白、空关键词与全空格输入通过。本人明确说明 `trim()` 是已用过但忘记了，不新增独立薄弱点。
- 后续复习：在下一个自然出现的输入场景中，观察能否独立组合读取、处理、校验和转换，不为此追加本项目功能。

### P002．方法调用返回后，外层仍会继续执行

- 日期：2026-09-13
- 项目 / 阶段：学习任务清单管理器 / 第一天
- 专题：方法调用、局部变量、分支与 return
- 独立情况：问题定位及执行过程经解释；本人主动提出使用 `return`，最终自行采用 `else` 修正。
- 主要问题：类别输入非法时递归调用 `setCategory()` 重新输入，但外层调用在内层结束后继续执行数字转换；外层的局部变量仍保存原来的非法文字。因此先输入字母、再输入正确类别，仍会在“存入成功”后报错。需要理解每次方法调用各自保留局部变量，重新调用方法不等于结束当前调用。
- 修改与验证：把数字转换放进校验成功的 `else` 分支；复测“非法类别 → 正确类别 → 输入用时 → 添加成功”不再报错。讨论中也确认，重新调用之后结束当前调用的 `return` 可以阻止外层继续转换旧输入。
- 掌握情况：已修正当前控制流问题；能够主动提出一种可行处理方向，完整调用过程的独立推演仍待后续验证。
- 2026-09-16 复测补充：修改任务和状态输入中，曾出现重试成功后仍转换外层旧输入的问题；本人说明此前已考虑过处理方式，本次漏写。补上 `return` 后，“非法输入 → 正确输入 → 正常返回”复测通过。只更新本条修正证据，不新增重复条目或推断为再次不理解。
- 后续复习：不看本次解释，用两次方法调用说明各自变量的值，以及内层结束后外层从哪里继续；在后续项目中验证。

### P003．正则字符范围与重复次数的准确含义

- 日期：2026-09-13
- 项目 / 阶段：学习任务清单管理器 / 第一天
- 专题：正则表达式的字符组与量词
- 独立情况：概念经解释后自行修改。
- 主要问题：讨论中曾混淆 `[0-3]` 与 `{0,3}` 的含义，并把重复次数范围理解为不包含端点。实际代码使用 `[0-9]{0,3}`，允许零次数字，因此空字符串通过格式检查，随后数值转换失败。这是符号含义的理解问题，与用时判断漏写等号的疏漏不同。
- 修改与验证：区分 `[0-3]` 表示一个取值为 0、1、2、3 的字符，`{0,3}` 表示前一项重复 0～3 次且包含两端；代码改用 `[0-9]{1,3}`。复测中，直接按回车被拒绝，不再进入空字符串的数值转换。
- 掌握情况：已澄清本次概念并修正代码；是否能脱离提示准确解释其他字符组和次数范围，待复习时确认。
- 后续复习：给出一个简单的新格式需求，先用文字说明“允许哪些字符、最少几次、最多几次”，再自己写出表达式并预测空串及两端长度的匹配结果。

### P004．组合查询：多个条件同时成立

- 日期：2026-09-16
- 项目 / 阶段：学习任务清单管理器 / 组合查询
- 专题：条件组合、AND 语义、逐轮筛选
- 独立情况：会单项判断；组合思路经提示后，由本人自行实现并修改。
- 主要问题：本人明确表示单个条件会判断，但多个条件同时生效时，只能想到列出所有条件组合，认为过于复杂而未实现。需要理解各项条件可以独立判断后取交集，不必枚举每种选项组合。
- 修改与验证：经“每项条件独立判断，最终同时满足”的提示，本人采用复制任务列表，再按所选条件逐轮删除不符合项的实现。名称使用包含匹配，未选择的条件不参与筛选，空关键词不限制名称。复测单条件、多个条件、三条件、输入顺序变化、条件无交集均得到预期结果；原任务列表与统计不受影响。
- 掌握情况：已完成当前实现并通过验证；能否脱离提示解释逐轮筛选为何等价于同时满足，以及迁移到其他场景，待后续验证。
- 后续复习：在下一个自然出现的筛选场景中，自行说明多个条件的组合规则和“不限”的处理，再独立实现；不要求重写当前方案。

### P005．遍历集合时删除元素

- 日期：2026-09-16
- 项目 / 阶段：学习任务清单管理器 / 查询筛选
- 专题：增强 for、结构修改、Iterator.remove
- 独立情况：本人提交报错代码询问原因；经解释后自行改用迭代器删除。
- 主要问题：在增强 `for` 遍历 `list1` 时调用 `list1.remove(task)`，未区分列表直接删除与遍历器的删除方式。实测既出现 `ConcurrentModificationException`，也出现未报错但漏检查元素的情况。
- 修改与验证：本人改为通过 `Iterator` 遍历，并使用该迭代器的 `remove()` 删除当前不符合条件的任务。名称、状态、类别筛选均采用该方式；连续排除多个任务、全部排除及多轮筛选复测通过。
- 掌握情况：当前实现已修正；“未报错不代表遍历正确”及遍历器与列表修改之间的关系，仍需在后续独立解释时确认。
- 后续复习：能解释为何增强 `for` 中直接删除列表可能出错，并在未来合适的场景中独立选择安全的遍历删除方式，不额外扩展本项目。

### P006．任务编号不能依赖当前集合大小

- 日期：2026-09-16
- 项目 / 阶段：学习任务清单管理器 / 添加与删除联动
- 专题：唯一标识、编号分配、跨操作业务规则
- 独立情况：审查指出删除后新增的问题；本人认可此前考虑不足，并自行修正。
- 主要问题：最初按添加后的 `list.size()` 分配编号，仅在一直添加时正确。实测添加 1、2、3 后删除 2 再新增，编号变成 1、3、3；删除全部再添加也会复用旧编号。这里记录的是“删除改变数量，但历史编号不能回退”的设计问题，与第一天已自行发现的编号差一疏漏分开。
- 修改与验证：改为由 `TaskService` 保存独立递增计数，每次成功添加时分配并增加，删除不回退。删除中间任务后新增得到新编号，删除全部后新增也不复用编号，两类检查均通过。
- 掌握情况：已识别当前记录数量与编号分配进度的区别，并修正本项目；后续能否独立考虑跨操作约束，待验证。
- 后续复习：在下一次需要唯一编号的功能中，先说明删除记录后哪些状态应该变化、哪些不应变化，再确定编号分配方式。

## 待复习内容

> 以下未勾选表示“独立掌握尚待验证”，不表示当前代码仍然有错，也不属于本项目尚未完成的任务。后续在新项目中自然复习即可。

- [ ] P001：在新的输入场景中，独立组合读取、文字处理、格式检查、转换和业务检查。
- [ ] P002：独立推演嵌套方法调用及返回位置，解释局部变量为何不会被内层同名变量替换。
- [ ] P003：独立区分字符组与重复次数，并准确判断端点和空字符串是否匹配。

- [ ] P004：独立说明多条件交集、“不限”的含义，并迁移到新的筛选场景。
- [ ] P005：独立解释遍历时直接修改集合的风险，选择安全的删除方式。
- [ ] P006：在有删除操作的情况下，独立设计不重复、不回退的编号分配规则。

## 后续项目记录模板

```markdown
### P编号．薄弱点名称

- 日期：
- 项目 / 阶段：
- 专题：
- 独立情况：自行发现 / 提示后自行修改 / 需要进一步帮助 / 待确认
- 主要问题：写清实际表现、本人反馈及其暴露的理解问题，不把小疏漏扩大成能力评价。
- 修改与验证：已改什么、实际验证了什么；未验证则明确说明。
- 掌握情况：稳定掌握 / 基本理解 / 需要复习 / 尚未掌握 / 待复习时确认，并附证据。
- 后续复习：用什么新的解释或应用场景验证；不直接给完整答案。
```

## 更新日志

- 2026-09-16：记录七项核心功能完成，最新代码编译与 29 项检查全部通过；本人决定以核心功能完成为终点，不再进行简短项目说明及其他剩余收尾。补充 P004 组合查询、P005 遍历删除、P006 编号分配，更新 P001 / P002 的复测证据。本人说明的忘记补写等小疏漏不另列为能力短板；修正通过不等同于稳定掌握。

- 2026-09-13：参照 LeetCode 档案的组织方式创建项目记录。依据学习任务清单管理器第一天的讨论与复测，记录输入处理、调用返回后的控制流、正则语义三项；注明本次已修正、独立迁移尚待验证。按照本人要求，不把漏写等号及已自行发现但忘记修改的编号疏漏列为薄弱点。
