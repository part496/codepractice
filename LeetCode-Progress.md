# LeetCode 刷题进度

## 使用规则

### 安排题目前必须核对

以后安排算法或 LeetCode 题目之前，必须先核对：

- 已经完成的题目；
- 当前数据结构课程进度；
- 每道题暴露的问题；
- 尚未复习或掌握不稳定的内容。

安排题目时：

- 不得重复安排已经完成的题目，除非明确标记为复习；
- 不得安排明显超出当前课程进度的内容；
- 难度必须循序渐进；
- 开始时只给题目，不提前提供提示、关键思路、算法标签或引导性问题；
- 只有在我主动请求提示或提交代码之后，才分析解法；
- 优先从灵茶山艾府的题单中筛选，但不能机械按照题单顺序安排；
- 数据结构课程进度作为当前主线；
- 双指针、二分等旧专题可以少量穿插复习，避免长期遗忘。

### 记录与更新规则

- “独立完成”指在没有查看提示、题解和关键思路的情况下完成。
- 如果只在修复小语法错误后通过，仍可视为独立完成。
- 如果得到关键算法提示后完成，应填写“部分独立”或“否”。
- 主要问题要记录真正暴露的知识或思维问题，不要只写“粗心”。
- 掌握情况应区分为：稳定掌握、基本理解、需要复习、尚未掌握。
- 后续复习后可以更新原记录，不要重复创建同一道题。
- 新安排题目之前，必须先核对本文件，避免重复题、超纲题和难度跳跃。
- 如果具体日期、是否独立完成或详细问题无法确认，分别填写“待补充”“待确认”“暂无详细记录”；掌握情况填写“待复习时确认”。

## 当前课程进度

- 当前跟随王卓的数据结构课程学习。
- 链表基础已经学过。
- 当前正在学习栈和队列。
- 栈和队列课程目前以理论和基础实现为主。
- 尚未系统学习树、图、堆、并查集、字典树、树状数组、线段树和动态规划等后续内容。
- 后续安排 LeetCode 题目时，不得安排明显超出当前课程进度的题目。

## 当前总体情况

- 双指针基础已经练习过，但滑动窗口和分组循环还没有系统学习。
- 已经做过 15、16、18，因此三指针不能列为“没有学习”。
- 普通二分已经有一定基础，二分答案也练习过最小化最大值和最大化最小值，但边界推导和独立识别答案单调性的能力仍不稳定。
- 链表基础操作比刚开始稳定，但指针关系、哑节点和特殊双指针技巧仍需间隔复习。
- 已经能够使用数组和 top 模拟栈，当前正在通过基础题巩固栈和队列。
- 在 1482、1552 等二分答案题的 check 过程中接触过局部贪心思想，但还没有系统刷贪心专题。
- 做过若干题目不代表整个双指针、二分或其他专题已经全部完成。

### 尚未系统学习或尚未完整覆盖的内容

- 定长滑动窗口
- 不定长滑动窗口
- 分组循环
- 单调栈
- 网格图 DFS / BFS
- 系统的位运算专题
- 系统的贪心专题（并非完全没有接触，曾在部分二分答案题的 check 中使用局部贪心思想）
- 图论
- 动态规划
- 树与二叉树
- 堆
- 字典树
- 并查集
- 树状数组
- 线段树
- 二分中的第 K 小类问题

## 已完成题目

> 本节记录已经完成或练习过的题目，不表示相应专题已经全部完成。

### 双指针

专题说明：

- 15、16、18 属于排序后固定一个位置，再使用左右双指针，也可以归入三指针类题目。
- 双指针基础已经练习过，但滑动窗口和分组循环还没有系统学习。
- 不能把整个双指针专题标记为已经全部完成。

#### 11. 盛最多水的容器
- 日期：待补充
- 专题：双指针
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 15. 三数之和
- 日期：待补充
- 专题：双指针 / 排序 / 三指针
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 16. 最接近的三数之和
- 日期：待补充
- 专题：双指针 / 排序 / 三指针
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 18. 四数之和
- 日期：待补充
- 专题：双指针 / 排序 / 三指针
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 125. 验证回文串
- 日期：待补充
- 专题：双指针
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 167. 两数之和 II - 输入有序数组
- 日期：待补充
- 专题：双指针
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

### 二分查找

专题总体问题：

- 对普通二分已经有一定基础。
- 容易混淆左中位和右中位。
- 容易混淆 left、right 应该更新为 mid、mid + 1 还是 mid - 1。
- 对循环条件和边界是否取等号有时不稳定。
- 能理解修改后的正确写法，但需要提高独立推导边界的能力。

#### 704. 二分查找
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 35. 搜索插入位置
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认
- 掌握情况：待复习时确认

#### 278. 第一个错误的版本
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 69. x 的平方根
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 33. 搜索旋转排序数组
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 34. 在排序数组中查找元素的第一个和最后一个位置
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 154. 寻找旋转排序数组中的最小值 II
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 162. 寻找峰值
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 74. 搜索二维矩阵
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 81. 搜索旋转排序数组 II
- 日期：待补充
- 专题：二分查找
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

### 二分答案

专题总体问题：

- 已经练习过最小化最大值和最大化最小值。
- 在专题环境下能够意识到使用二分答案。
- 离开专题后，独立识别“答案具有单调性”仍不稳定。
- check 函数有时写得过于复杂。
- 曾在求最大值时因为使用左中位和 left = mid 导致死循环。
- 已经理解求最大值时经常使用右中位：`mid = left + (right - left + 1) / 2`。
- 仍需复习为什么加 1 可以取得右中位，以及它与边界更新方式的配合。

#### 875. 爱吃香蕉的珂珂
- 日期：待补充
- 专题：二分答案
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 1283. 使结果不超过阈值的最小除数
- 日期：待补充
- 专题：二分答案
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 1011. 在 D 天内送达包裹的能力
- 日期：待补充
- 专题：二分答案
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 1482. 制作 m 束花所需的最少天数
- 日期：待补充
- 专题：二分答案 / 局部贪心
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

#### 1552. 两球之间的磁力
- 日期：待补充
- 专题：二分答案 / 局部贪心
- 是否独立完成：待确认
- 主要问题：暂无详细记录
- 掌握情况：待复习时确认

### 链表

专题总体问题：

- 对 head、head.next、当前节点和下一个节点的含义曾经不稳定。
- 修改 next 指针时有时容易在脑中混乱。
- 对哑节点的使用还不够自然。
- 能理解快慢指针、双指针换头等方法，但独立想到这些方法的能力不足。
- 基础删除、跳过和反转操作已经比刚开始稳定，但仍需要间隔复习。

#### 203. 移除链表元素
- 日期：待补充
- 专题：链表
- 是否独立完成：待确认
- 主要问题：最开始容易把“删除节点”理解为把节点设置为 null，对首节点的处理比较困难。
- 掌握情况：后来能够通过修改 next 指针删除节点，但对哑节点的使用还不够自然。

#### 83. 删除排序链表中的重复元素
- 日期：待补充
- 专题：链表
- 是否独立完成：是
- 主要问题：这道题与 203 的指针跳过操作相似。
- 掌握情况：大约五分钟独立完成，说明基础的 p.next 修改已经比之前稳定。

#### 206. 反转链表
- 日期：2026-07-25；复习：2026-09-01
- 专题：链表
- 是否独立完成：首次部分独立；2026-09-01 复习独立完成
- 主要问题：首次学习阶段曾混淆 head、head.next 和节点本身的含义，对“反转链表本质上是修改 next 指针方向”的理解需要外部引导。2026-09-01 间隔复习时没有查看提示或旧代码，约十分钟独立完成；自行使用 `cur`、`next`、`save` 保存旧后继并逐个反转指针，最后正确执行 `head.next = null` 处理新尾节点，全程没有出现成环、断链或节点丢失。
- 掌握情况：基础迭代反转已经能够脱离提示独立完成，且能正确处理修改指针前保存旧关系和尾节点断链；本次已验证不是只记住旧代码，206 当前可视为稳定掌握，不需要近期专门复习。

#### 21. 合并两个有序链表
- 日期：待补充
- 专题：链表
- 是否独立完成：待确认
- 主要问题：最初不确定应该新建头节点还是直接操作原链表；曾把较大的节点接入结果；也曾错误处理 head = list1 或 head = list2。
- 掌握情况：后来理解哑节点和尾指针的作用，也理解可以复用原链表节点，但哑节点的使用还需要继续巩固。

#### 876. 链表的中间结点
- 日期：待补充
- 专题：链表 / 快慢指针
- 是否独立完成：待确认
- 主要问题：最开始通过统计链表长度再取中间位置，效率较低；对快慢指针为什么在偶数长度时返回右中位不能立即解释。
- 掌握情况：后来能够写出快慢指针解法，并通过实际推导理解偶数长度返回右中位。

#### 160. 相交链表
- 日期：待补充
- 专题：链表 / 双指针
- 是否独立完成：待确认
- 主要问题：最开始加入 flag 和额外的 null 判断，导致逻辑复杂且出错；理解交换链表后两指针路程相等的原理花了较长时间。
- 掌握情况：已经理解两个指针分别走 A + B 和 B + A 后路程相同，但该方法较难独立想到，需要之后复习。

#### 2181. 合并零之间的节点
- 日期：2026-08-11
- 专题：链表 / 原地修改 / 区间累加
- 是否独立完成：是
- 主要问题：能够独立写出可行解法，但实现用了较多指针，并在遇到分隔 0 后再次遍历区间求和，代码状态偏复杂；讨论指针调整顺序时一度忽略第一组的特殊初始状态，若提前执行 `ago.next = pre.next`，第一组会因为 `pre.next == ago` 形成自环。
- 掌握情况：已经能独立处理较复杂的链表连接，也能在指出现象后自己定位到“第一次状态特殊”这一原因；但还需要训练单次遍历中直接维护区间和、减少指针数量，以及修改 `next` 前检查多个引用是否指向同一节点。

#### 725. 分隔链表
- 日期：2026-08-12
- 专题：链表 / 切分链表 / 指针操作
- 是否独立完成：是
- 主要问题：整体逻辑正确，能够先统计长度，再按 `count / k` 和 `count % k` 分配每段长度，并正确保存每段头节点、找到尾节点、保存下一段起点后断链；对 `ListNode[]` 中元素保存的是节点引用这一点曾单独确认。变量 `pre` 实际表示当前分段头节点，命名略容易与前驱节点混淆。
- 掌握情况：链表切分和断链操作较稳定，能够独立处理 `k >= count` 与 `k < count` 两种情况，没有出现自环、断链或节点丢失；说明当前基础链表指针操作比之前明显稳定。

#### 328. 奇偶链表
- 日期：2026-08-15
- 专题：链表 / 原地重连 / 双链维护
- 是否独立完成：是
- 主要问题：约十分钟独立完成，没有出现明显的指针错误；能够保存偶数链起点 `evenOrigin`，在遍历中分别维护奇数链和偶数链，并在最后正确拼接。循环条件写得比必要条件更复杂，但不影响正确性。
- 掌握情况：完成明显偏轻松，说明当前基础链表重连、保存后续链入口以及多指针推进已经较稳定；这类只考基础 `next` 重连的题目后续不应高频安排，需要逐步提高链表题的状态复杂度。

#### 143. 重排链表
- 日期：2026-08-17
- 专题：链表 / 原地重连 / 链表反转组合
- 是否独立完成：是（提交通过，但实现为 O(n²)）
- 主要问题：能够独立按目标顺序不断把当前尾节点插入前半部分，并利用临时形成的环从已插入的 `right` 出发顺着 `next` 找到原尾节点前驱，因此实现逻辑本身正确；但每轮都要再次线性寻找尾部前驱，总复杂度为 O(n²)。循环次数使用 `amount /= 2` 与 `count` 控制并不是从明确的不变量或指针相遇条件推导出来，而只是观察到数值上正好匹配，因此虽然通过测试，自己无法解释为什么循环应执行这些次。
- 掌握情况：基础的节点插入与复杂重连已经能够独立完成，说明 `next` 操作本身较稳定；当前更需要训练在写循环前明确“每轮确定了什么、何时应该停止”，并积累把已学过的操作组合成更高效方案的能力。后续应重新独立完成一次线性解法，重点复习找中点、反转后半链表和两段交替合并的组合。

#### 82. 删除排序链表中的重复元素 II
- 日期：2026-08-20；复习：2026-08-31
- 专题：链表 / 多指针 / 重复段删除 / 边界处理 / 哑节点
- 是否独立完成：部分独立（2026-08-31 复习时主体独立完成，边界与状态推进仍需少量帮助）
- 主要问题：首次练习时能快速识别多指针方向，但 `pre`、当前组、扫描指针的职责和重复段结算时机不稳定，也没有自然使用哑节点。2026-08-31 复习时已经主动创建虚拟头节点 `preHead`，说明哑节点意识明显进步；但一开始仍额外维护 `head`，并对 `second == head` 写了首元节点特殊分支。处理 `[1,1,2,2]` 时，该分支在把 `flag` 设为 `false` 后执行 `continue`，导致本轮后续的 `second = third` 和 `third = third.next` 都被跳过；下一轮出现“flag 已进入新组状态，但 second 仍停在旧组”的状态不同步。自己手推时发现了 `third` 没有推进，但没有同时注意到 `second` 也没有推进。随后理解哑节点已经提供了原首节点的前驱，因此可直接删除 `second == head` 的特判，用统一的 `first.next = third` 处理头部与中间重复段，并通过 `preHead.next` 作为结果入口；删除特判后代码通过。
- 掌握情况：相比第一次，已经能主动使用哑节点，且重复组主体逻辑基本能够独立完成；“哑节点用于统一原首节点没有前驱的特殊情况”已经理解。当前仍需注意 `continue` / `break` 前后多个状态变量是否同步推进，尤其多指针代码中不能只检查一个指针。82 已完成一次间隔复习，但暂不视为稳定掌握。

#### 92. 反转链表 II
- 日期：2026-08-23
- 专题：链表 / 区间反转 / 指针重连 / 哑节点
- 是否独立完成：是（过程中因题意误读与指针关系 bug 调试较久）
- 主要问题：开始把 `left`、`right` 误理解为节点值，而不是从 1 开始计数的位置，前期实现因此偏离题意；纠正后能够自行定位反转区间并完成主体反转。真正卡住的是反转结束后的尾部重连：先修改了 `fast.next`，之后又用 `fast.next` 试图取得原来的 `right` 后继，忽略了节点引用指向同一对象且 `next` 已经被改写，导致形成环并丢失后续链表。经指出后意识到反转循环结束时 `cur` 已经保存了原区间后继，改为 `temp.next = cur` 后 AC。
- 掌握情况：局部反转主体与基础 `next` 操作已经能够独立完成；当前更需要固定“修改指针前先保存之后还要使用的旧关系”，以及修改后不能把节点引用当成旧状态快照。对区间到链尾无需额外特殊处理的判断正确；后续应隔一段时间不看本次代码重做一次，并尝试让哑节点统一处理头部边界。

#### 2. 两数相加
- 日期：2026-08-28
- 专题：链表 / 哑节点 / 双链遍历 / 进位
- 是否独立完成：是
- 主要问题：整体完成较快，能够直接使用哑节点和尾指针构造结果链表，并分别处理“两条链表都有节点”“只剩 l1”“只剩 l2”三个阶段，说明这一类三段式链表流程已经很熟。实现中三段循环存在较多重复代码，可以进一步把“某条链表不存在时当前位视为 0”统一到一个循环中；另外进位判断写成了 `num < 9`，语义上应为 `num < 10`，但 `num == 9` 时落入 `% 10` 和 `/ 10` 分支仍得到正确结果，因此没有造成 WA。
- 掌握情况：哑节点、尾插、双链同步推进和十进制进位已经表现稳定，这题对当前水平偏基础，不需要高频复习；后续更值得训练的是在保证正确后主动识别重复控制流并简化实现。

### 栈

专题总体情况：

- 已经能够使用数组和 top 模拟栈。
- 对 top 表示栈中元素数量、同时指向下一个可写位置的理解正在逐渐稳定。
- 能够处理入栈、撤销顶部元素、读取前两个元素等操作。
- 代码有时仍可以简化，需要继续关注边界和表达清晰度。

#### 20. 有效的括号
- 日期：待补充
- 专题：栈 / 数组模拟
- 是否独立完成：待确认
- 主要问题：最开始用 char 数组和 top 手动模拟栈，但判断逻辑写得比较绕。
- 掌握情况：已经理解数组加 top 就是在模拟栈；课程当时主要讲了理论，还没有系统教授 Java 标准栈写法。

#### 1047. 删除字符串中的所有相邻重复项
- 日期：待补充
- 专题：栈 / 数组模拟
- 是否独立完成：待确认
- 主要问题：调试过程中一度混淆“字符已经进入数组”和“字符只参与判断但没有入栈”；对 top 指向下一个空位置的含义需要进一步稳定。
- 掌握情况：已经能用 char 数组和 top 模拟栈完成；这道题与之前的栈题结构相似，但休息两天后仍花了约二十分钟，需要复习熟练度。

#### 1209. 删除字符串中的所有相邻重复项 II
- 日期：2026-08-24；复习：2026-08-31
- 专题：栈 / 数组模拟 / 平行数组 / 附加状态
- 是否独立完成：首次部分独立；2026-08-31 复习独立完成
- 主要问题：首次练习时已经意识到需要额外记录次数，也考虑过增加数组，但没有独立建立 `count[i]` 与 `str[i]` 同下标绑定的状态模型；当时容易把次数理解为“某个字符总共出现多少次”，并在控制流中出现同一字符重复入栈的问题。2026-08-31 间隔一周复习时约十分钟独立 AC，能够直接使用 `char[] str` 与 `int[] count` 平行维护同一逻辑栈元素：相同字符入栈时自然写出 `count[top] = count[top - 1] + 1`，达到 `k` 时通过调整 `top` 删除整组，整个过程中 `[0, top)` 始终保持为有效栈区，没有再出现搜索前面字符、额外 flag 或重复入栈。
- 掌握情况：“栈元素可以由值 + 附加状态共同描述”的建模已经从“提示后能实现”提升为能够独立使用；平行数组同下标绑定状态、`top` 有效区间和连续计数本次表现稳定。该题当前可视为基本掌握，不需要近期高频复习；后续若遇到新的附加状态栈题，再观察能否迁移这一建模习惯。

#### 682. 棒球比赛
- 日期：2026-08-06
- 专题：栈 / 数组模拟
- 是否独立完成：是
- 主要问题：没有明显算法错误，能够用数组和 top 保存有效分数，并同步维护总分。
- 掌握情况：完成得较稳定；是否记录到 GitHub 需要根据代表性决定，不应只因为独立完成就全部记录。

#### 946. 验证栈序列
- 日期：2026-08-08
- 专题：栈 / 数组模拟
- 是否独立完成：是
- 主要问题：前期反复纠结是否需要额外数组来维护栈状态；后期把 `top` 当成栈顶下标使用，曾写成 `nums[top]`，而当前写法中 `top` 实际表示栈中元素数量并指向下一个可写位置，所以栈顶应为 `nums[top - 1]`。这个下标错误导致调试耗时很长；另外为“刚入栈就立即出栈”的情况增加了特殊分支，代码状态较多。
- 掌握情况：能够独立建立入栈/出栈模拟并最终写出可行代码，说明栈过程本身已经理解；但 `top` 的语义仍不够稳定，需要继续训练统一的栈操作写法，并减少不必要的特殊分支。

#### 844. 比较含退格的字符串
- 日期：2026-08-11
- 专题：栈模拟 / 逆向双指针
- 是否独立完成：是（StringBuilder 栈模拟）；O(1) 空间优化为部分独立
- 主要问题：第一版能够独立用 StringBuilder 模拟入栈和退格删除，但用时较长。继续挑战 O(1) 空间解法时，已经想到从字符串末尾向前比较，但一度认为退格计数器和简单 `top--` 等价，因此把计数器删掉；同时误以为外层加两个内层 `while` 会因为三层循环而更慢。后来理解退格计数器用于记录“还需要跳过多少个普通字符”，也理解复杂度应看指针总移动次数而不是循环层数。最终主体结构已经搭出，但对某一侧提前扫到 -1 后的边界判断仍未独立处理完整。
- 掌握情况：栈模拟本身较稳定，`top`/有效元素数量的语义比之前清楚；已经基本理解逆向双指针的 O(1) 思路，但需要在状态较好时重新独立写一次，重点确认退格计数器、嵌套 while 的线性复杂度以及两侧长度不一致时的边界。

#### 2390. 从字符串中移除星号
- 日期：2026-08-13
- 专题：栈 / StringBuilder 模拟
- 是否独立完成：是
- 主要问题：没有明显算法问题；能够直接把 StringBuilder 当作栈，普通字符 `append`，遇到 `*` 删除栈顶字符。整体完成较快，与此前 1047、844 等题的栈模拟结构相近。
- 掌握情况：这类字符串栈模拟目前表现稳定，已经能够快速复现，不需要继续高频安排近乎同构的基础题。

#### 735. 小行星碰撞
- 日期：2026-08-15
- 专题：栈 / 数组模拟 / 连续碰撞状态
- 是否独立完成：部分独立
- 主要问题：主体思路和“当前负小行星需要连续与栈顶正小行星碰撞”的逻辑能够自行建立，也已经意识到当前 `num` 不能过早进入栈；但在写了约一个半小时后没有继续重构，原实现使用 `asteroids[--top] = num` 把仍在处理的 `num` 提前写入栈区，导致数组中的值与 `top` 所定义的有效区间不一致，随后又用 `flag` 和额外的 `top++` 修补状态。经指出应把“当前元素是否仍存活”和“是否最终入栈”分开后，独立改成只在碰撞彻底结束后统一入栈。相等碰撞时还自行通过 `num = 0` 阻止已经被撞碎的小行星继续参与后续碰撞，之后理解用 `break` 表达控制流更直接。
- 掌握情况：栈模拟本身已经有基础，真正需要加强的是复杂状态下保持 `top` 语义唯一：`asteroids[0...top-1]` 必须始终表示已经处理完成且仍存活的栈元素，当前正在处理的 `num` 不应提前混入有效栈区。还需要训练在发现状态定义错误后及时重构，而不是继续用标志位和特殊分支补丁式修复。

#### 71. 简化路径
- 日期：2026-08-19
- 专题：栈 / 字符串分割 / 路径规范化
- 是否独立完成：否（字符级方案独立尝试约两小时，最终参考并直接提交标准解法）
- 主要问题：不知道 `String.split()`，因此把路径按字符处理，用 `char[] + top` 保存结果，同时再用 `slash[] + j` 记录斜杠位置，导致状态数量迅速膨胀；过程中能够自己发现原字符串下标 `i` 与处理后数组位置不同，并把 `slash[j++] = i` 修正为记录 `top`，这一点判断正确。但之后为了区分 `./`、结尾 `.`、`../`、结尾 `..`、重复 `/` 等情况不断增加特殊分支，且多次混淆“当前字符还没入栈”和“已经位于有效栈区”的状态。最终没有识别出题目的自然处理单位应是完整目录/token，而不是单个字符。
- 掌握情况：已经理解 `split("/")` 可以把路径拆成目录段，也理解增强 for `for (String part : parts)` 的基本含义；能够理解空段和 `.` 直接忽略、`..` 弹出一个目录、普通目录入栈的标准模型。当前属于看懂解法而非独立掌握，需要之后不看答案重新完成一次，重点确认“先选对状态单位，再设计栈状态”。

#### 155. 最小栈
- 日期：2026-08-20；复习：2026-09-01
- 专题：栈 / 辅助栈 / O(1) 状态维护 / 设计题
- 是否独立完成：首次部分独立；2026-09-01 复习独立完成（算法设计独立）
- 主要问题：首次练习时没有从 O(1) 查询要求独立推出辅助栈，得到“双栈保存最小值”的关键提示后才完成。2026-09-01 复习时已经能够不看提示重新设计普通栈与最小栈，并正确使用 `value <= min` 让重复最小值也进入 `minStack`，弹出当前最小值时同步回退最小栈，说明此前的双状态同步问题没有再次出现。本次唯一导致测试失败的问题是 Java 整数边界常量写法：把最大 int 写成 `(int)Math.pow(2,31) - 1`，实际先把超出 int 范围的 `2147483648.0` 强转为 `2147483647`，再减 1 得到 `2147483646`，导致后续 `push(2147483647)` 无法进入最小栈；改用 `Integer.MAX_VALUE` 即可。该问题属于 Java 数值转换与边界常量使用，不是最小栈算法设计错误。
- 掌握情况：已经能够独立建立辅助栈方案并维持普通栈、最小栈同步，O(1) 维护最小值的核心思路本次表现稳定；当前可视为基本掌握，不需要近期再次专门复习。Java 中整数极值以后直接使用 `Integer.MAX_VALUE` / `Integer.MIN_VALUE`，避免用 `Math.pow()` 构造。

#### 856. 括号的分数
- 日期：2026-08-28
- 专题：栈 / 嵌套结构 / 层级状态 / 表达式求值
- 是否独立完成：部分独立
- 主要问题：独立尝试约一小时，最初仍沿用此前“栈保存题目中的实体”的习惯，用 `char[]` 保存左括号，再用 `num`、`numB`、`count` 额外维护分数。真正卡点是 `A + B`：能够意识到需要保存前一个值，但单个 `numB` 无法表达多层嵌套中每一层尚未完成的累计结果，也一直试图判断当前究竟应该执行加法还是乘 2。经提示后才建立“每个括号深度都有自己的累计分数”的状态模型：遇到 `(` 新建一层并置 0；遇到 `)` 时当前层为 0 则这一对为 1，否则整层变为 `2 * cur`，随后无条件把该值加到父层。第一次实现这一模型时又缺少最外层总分容器，导致最外层括号关闭后分数无处保存，之后理解可以额外保留 `count[0]` 作为虚拟最外层。
- 掌握情况：已经理解栈不仅可以保存括号、字符、小行星等实体，也可以保存“某一层计算进行到什么程度”的中间状态；也理解加法与乘法不是二选一，一次闭括号可能先完成当前层的乘法，再把结果累加到父层。这个建模方式目前属于理解后能够复现，但很难独立想到，是当前栈专题最值得间隔复习的新知识点。

### 队列

专题总体问题：

- 容易把题目要求的队列过程完整地机械模拟出来。
- 可能尝试删除数组元素、移动数组或真的让队首元素不断出队再入队，导致代码复杂。
- 需要培养识别“题目虽然描述队列，但不一定必须真正维护完整队列”的能力。
- 应优先观察是否可以通过计数、双指针、front/rear 或数学统计简化。
- 对类成员变量、构造方法和对象状态刚刚重新熟悉。
- 对“已知最终结果，反推初始状态”的逆向模拟还不熟悉。
- 使用数组手动模拟队列变化时，元素搬移方向和有效区间边界容易混乱。
- 对循环队列中 `front`、`rear` 的基本含义已经能较快复现，但需要牢固理解“空/满判定方式”和内部数组容量之间的关系。

#### 933. 最近的请求次数
- 日期：2026-08-05
- 专题：队列 / 类与对象
- 是否独立完成：是，但思考时间较长
- 主要问题：很久没有写类和对象，忘记了 RecentCounter() 是构造方法；一开始不敢把 time、front、rear 写在 ping 方法外面，不清楚对象需要在多次方法调用之间保存状态。
- 掌握情况：后来通过向 ChatGPT 描述思路时把逻辑理清并写出代码；已经重新理解成员变量和构造方法的作用，但需要继续练习有状态对象题。

#### 2073. 买票需要的时间
- 日期：2026-08-07
- 专题：队列 / 模拟
- 是否独立完成：否
- 主要问题：把队列模拟得过于复杂，尝试删除元素并移动数组
- 掌握情况：理解直接统计或简单模拟，之后需要复习

#### 950. 按递增顺序显示卡牌
- 日期：2026-08-08
- 专题：队列 / 逆向模拟 / 数组模拟
- 是否独立完成：部分独立
- 主要问题：一开始完全没有从目标结果反推初始排列的思路；在得到“把正向操作倒过来”的关键提示后理解了逆向模拟，并能按从大到小恢复牌堆。随后在数组实现中，保存队尾时误用了 `deck` 而不是当前状态数组 `nums`，同时对整体右移时的起止下标和移动方向处理不稳定。
- 掌握情况：已经理解核心的逆向模拟思想，也能解释正向“队头移到队尾”对应反向“队尾移到队头”；但目前还不能独立想到该思路，数组搬移边界也不稳定。需要在状态较好时重新独立推导一次，再决定是否作为代表题整理到 GitHub 笔记。

#### 1700. 无法吃午餐的学生数量
- 日期：2026-08-12
- 专题：队列 / 循环模拟 / 计数
- 是否独立完成：是
- 主要问题：相比 2073 已经避免删除数组和整体搬移，使用环形下标与 `-1` 标记离队学生完成模拟，说明过度模拟问题有所改善；`studentsRear` 实际没有参与任何判断，可以删除。停止条件最开始自行推导成 `count > 剩余人数`，后来讨论后确认连续失败次数达到剩余人数时就已检查完整一轮，因此 `>=` 更直接；这属于一次 off-by-one 推导偏差，而不是靠测试碰出来的修改。
- 掌握情况：能够独立完成队列过程模拟，且实现复杂度比 2073 明显降低；仍需继续训练让计数变量的语义与停止条件直接对应，减少无效状态和边界推导负担。

#### 622. 设计循环队列
- 日期：2026-08-13
- 专题：循环队列 / 数组 / front-rear
- 是否独立完成：部分独立
- 主要问题：整体循环队列结构能够较快复现，已经知道 `rear` 表示“下一次要填充的位置”，也能使用 `front == rear` 判空和 `(rear + 1) % maxSize == front` 判满；但忘记了这种判空/判满设计必须故意牺牲一个数组位置，因此 LeetCode 要求队列实际容量为 `k` 时，内部数组长度应为 `k + 1`。这一点笔记里其实记过，但没有形成稳定记忆。初版 `Rear()` 和 `deQueue()` 还各有一个很快发现并修正的小实现失误，修改后已通过提交。
- 掌握情况：循环队列主体逻辑基本理解，当前最需要固定的是：当 `rear` 指向下一次插入位置、并用 `front == rear` 判空时，为区分队空和队满必须保留一个空槽；因此“可存 `k` 个元素”需要内部数组开 `k + 1`。该知识点需要之后不看笔记重新解释一次。

#### 649. Dota2 参议院
- 日期：2026-08-19
- 专题：队列 / 双队列 / 数组模拟
- 是否独立完成：是（使用两个数组手动模拟队列）
- 主要问题：能够把 Radiant 和 Dire 的下标分别放入两个队列，并维护各自的 `front`、`rear`、`size`；比较两个队首下标后，让先行动的一方进入下一轮，并通过给下标加 `senate.length()` 表示其下一次行动顺序，主体算法能够自行实现。之后询问了使用 Java 队列 API 的标准写法，说明当前主要缺口不是队列思想，而是尚不熟悉 `Queue` / `Deque` 等标准库接口。
- 掌握情况：双队列按时间顺序模拟的思想基本掌握，手写数组队列也能够完成；后续不需要继续为了“证明会队列”而重复手搓全部底层状态，可以逐步补 Java 标准队列 API，并学会在算法关注点与容器实现细节之间做取舍。

#### 225. 用队列实现栈
- 日期：2026-08-21
- 专题：队列 / 设计题 / Queue API / 辅助队列
- 是否独立完成：是（此前未学过 Java Queue API，先补充 API 含义后独立完成）
- 主要问题：起初没有整体思路，先想到用 `num` 记录最近压入值，随后受前一日最小栈“双容器保存中间状态”的启发，逐步想到用第二个队列搬运元素。第一版 `pop()` 每次 `new Queue`，触发题目“最多两个底层元素容器”的限制；同时循环条件中直接读取不断变化的 `queue.size()` 会使搬运次数少于预期，后改为先固定 `count`。最终版本 AC。`queue1 = queue2` 是有意让 `queue1` 指向 `queue2` 所引用的同一对象，以修复后续状态，并非不理解 Java 引用赋值；但当时没有继续推演两个引用合流后的行为，因此没有意识到后续 `poll` / `offer` 已经实际变成了对同一个队列的旋转，最终代码无意中混合了“两队列搬运”和“单队列旋转”两种正确方案。
- 掌握情况：已经会使用 `offer`、`poll`、`peek`、`isEmpty`、`size` 等基础 Queue API，也能够独立完成 FIFO 模拟 LIFO 的核心转换。当前更需要加强的是：修改容器或引用关系后继续向后推演一轮状态，并尽量让设计题的实现策略保持单一、可解释。

## 当前主要薄弱点

1. 独立识别题型的能力弱于根据提示复现解法的能力。
2. 看到答案后容易觉得“自己本来应该会”，但这不代表能够独立想出。
3. 链表指针关系在复杂情况下容易混乱；基础反转已于 2026-09-01 复习验证为稳定，后续重点放在复杂多指针和组合题，而不是重复基础反转。
4. 哑节点的使用已经比早期自然，82 的 2026-08-31 复习中能够主动创建哑节点；但仍可能在已经有哑节点后继续对原首节点写额外特殊分支，需要进一步形成“先判断哑节点是否已经统一了头部边界”的习惯。
5. 对快慢指针、链表换头等技巧能够理解，但不容易独立发现。
6. 二分法中的循环条件、中位数选择和边界更新容易混淆。
7. 二分答案的 check 函数有时写得过于复杂。
8. 栈已经可以使用数组和 top 模拟，但复杂连续处理时仍需保持 `top` 语义唯一：有效栈区必须始终是 `[0, top)`，当前正在处理、尚未确定存活的元素不要提前写入有效栈区；发现状态模型不对时应优先重构，而不是用额外 flag 修补。
9. 队列题容易过度模拟，但 1700 中已经比 2073 明显简化；仍需继续练习用更直接的状态和计数表达停止条件。
10. 对 Java 类成员变量、构造方法和对象状态的使用曾经生疏。
11. 中断学习一两天后，熟练度可能明显下降，需要安排间隔复习。
12. 做题时容易在一个实现细节上耗费很长时间，需要区分知识问题和一时的实现失误。
13. 不应只记录做得好的题，做错、做复杂或没有独立完成的题更需要记录。
14. 第一次遇到逆向构造/逆向模拟时不容易主动想到，需要积累“正向难构造时尝试反向恢复”的经验。
15. 数组整体搬移时，对从左到右还是从右到左以及有效区间边界的判断仍不稳定。
16. 看到嵌套循环时容易直接按循环层数判断复杂度，需要改为统计每个指针或元素的总移动次数。
17. 链表中间状态推导时容易忽略第一轮/头节点附近的特殊初始关系；修改 `next` 前要检查多个引用是否实际指向同一节点，避免自环或断链。
18. 链表基础切分与断链在 725 中表现稳定，328 中基础双链重连也完成得较轻松；后续链表题应减少纯基础重连题，逐步增加状态复杂度，同时变量命名仍应直接体现节点角色。
19. 循环队列中若 `rear` 表示下一次插入位置，并使用 `front == rear` 判空，则必须通过牺牲一个存储单元来区分队空与队满；对“实际容量 `k` 对应内部数组长度 `k + 1`”这一关系需要固定下来。
20. 143 重排链表中能够独立完成较复杂重连，但循环次数依赖“数值上正好对得上”而非从状态不变量推导；后续需要加强终止条件的解释能力，并避免为了获取单链表前驱而反复线性扫描。
21. 71 简化路径暴露出“处理单位选择”问题：遇到字符串结构题时容易直接按字符建模，而没有先判断逻辑对象是否应是单词、目录或 token；如果状态变量和特殊分支快速增多，应优先重新检查建模单位，而不是继续补丁式修复。
22. Java 刷题常用 API 与语言细节仍有缺口，例如 `String.split()`、增强 for，以及 2026-09-01 在 155 中暴露的整数边界常量写法；缺少这些工具或细节时容易让本来正确的算法实现出现额外问题。整数极值优先使用 `Integer.MAX_VALUE` / `Integer.MIN_VALUE`，不要用 `Math.pow()` 手算。
23. 链表多指针题中，主体方向已经比早期稳定，但多个状态变量的同步推进仍会出错。82 的 2026-08-31 复习中，`continue` 前已经修改 `flag`，却跳过 `second`、`third` 的推进，形成不同步状态；以后遇到 `continue` / `break`，需要逐个核对本轮所有状态变量哪些已改、哪些尚未改。
24. 第一次遇到由复杂度要求反推数据结构设计时不容易主动想到“额外维护状态”；155 首次需要提示才能建立辅助栈，但 2026-09-01 已能不看提示独立重新设计并保持双栈同步。该点已有明显改善，后续观察能否迁移到新的设计题，不再把 155 本身作为近期高优先级复习项。
25. 225 中已经能够独立使用 Queue API 和辅助队列完成设计，但修改引用关系后没有继续完整推演后续操作，导致最终代码无意中从“两队列搬运”切换成“单队列旋转”；后续设计题应在改变状态关系后再手动模拟至少一轮。
26. 1209 首次暴露的“给栈元素附加状态”建模问题在 2026-08-31 复习中已有明显改善：间隔一周后约十分钟独立完成，能够自然使用平行数组让 `count[i]` 与 `str[i]` 同位置绑定。该点暂时不再作为高优先级薄弱项，后续重点观察能否迁移到新的附加状态栈题。
27. 856 进一步暴露出对“栈保存抽象状态”还不自然：此前习惯让栈存括号、字符、小行星等实体，遇到嵌套求值时没有主动想到让每个栈位置保存该层累计结果；同时容易把 `A + B` 和 `2 * A` 看成需要二选一的操作，而没有先区分“当前层结算”和“把当前层结果累加到父层”这两个连续步骤。

## 待复习内容

- [x] 206. 反转链表：2026-09-01 不看提示约十分钟独立完成，基础迭代反转已验证稳定；近期无需重复复习。
- [ ] 160. 相交链表：重新解释为什么两个指针最终会相遇
- [ ] 876. 链表的中间结点：重新推导偶数长度为什么返回右中位
- [ ] 二分查找：左中位和右中位的选择
- [ ] 二分答案：left = mid 时为什么需要右中位
- [ ] 933. 最近的请求次数：重新写一个使用成员变量保存状态的类
- [ ] 1047. 删除相邻重复项：确认 top 的含义
- [ ] 2073. 买票需要的时间：避免完整队列模拟，尝试写出简化解法
- [ ] 946. 验证栈序列：重新用统一的“先入栈，再尽可能出栈”结构实现，确认 `top` 与 `top - 1` 的含义
- [ ] 950. 按递增顺序显示卡牌：在不看提示的情况下重新推导逆向模拟，并独立处理数组搬移边界
- [ ] 2181. 合并零之间的节点：尝试用单次遍历直接维护区间和，减少指针数量；重新解释第一组为什么可能形成自环
- [ ] 844. 比较含退格的字符串：不看提示重新写 O(1) 逆向双指针版本，确认退格计数和两侧提前结束的边界
- [ ] 1700. 无法吃午餐的学生数量：重新解释为什么连续失败次数达到剩余学生数时即可结束，确认 `>=` 的边界含义
- [ ] 622. 设计循环队列：不看笔记重新解释为什么 `front == rear` 判空时需要牺牲一个空槽，以及为什么题目容量为 `k` 时内部数组需要 `k + 1`
- [ ] 735. 小行星碰撞：不看本次代码重新实现一次，重点确认当前元素在碰撞彻底结束前不进入有效栈区，以及 `top` 始终表示 `[0, top)` 的有效元素数量。
- [ ] 143. 重排链表：不看本次代码重新写线性解法，重点确认如何由“找中点 + 反转后半链表 + 两段交替合并”组合得到 O(n)，并能解释循环何时结束。
- [ ] 71. 简化路径：隔一段时间不看答案重新完成；先把路径按目录/token 处理，再用栈维护目录层级，确认能独立使用 `split()` 而不是复现本次答案。
- [ ] 649. Dota2 参议院：以后用 Java 标准队列 API 再实现一次，重点熟悉队列接口，而不是重复证明数组手写队列。
- [x] 82. 删除排序链表中的重复元素 II：2026-08-31 已完成一次间隔复习；已能主动使用哑节点并理解其统一头部边界的作用，但本次仍出现 `continue` 导致多指针状态不同步，暂不视为稳定掌握。
- [x] 155. 最小栈：2026-09-01 不看提示独立重新设计双栈方案，状态同步正确；测试失败仅由 `(int)Math.pow(2,31) - 1` 的 Java 边界常量写法导致，核心算法当前基本掌握。
- [ ] 225. 用队列实现栈：不看本次代码，明确选择“纯双队列搬运”或“纯单队列旋转”其中一种写法，并能逐轮解释引用关系和队列状态。
- [x] 1209. 删除字符串中的所有相邻重复项 II：2026-08-31 间隔一周后约十分钟独立完成；能够独立建立 `str[i] + count[i]` 的附加状态模型，当前基本掌握。
- [ ] 856. 括号的分数：隔一段时间不看本次标准代码重新完成；先自己提出“每一层需要保存什么状态”，独立推出用栈保存每层累计分数，并能解释为什么闭括号时可能先做 `2 * cur`，随后还要把结果 `+=` 到父层。

## 后续刷题记录模板

```markdown
## 题号. 题目名称
- 日期：YYYY-MM-DD
- 专题：
- 是否独立完成：是 / 否 / 部分独立
- 主要问题：
- 掌握情况：
```

## 更新日志

- 2026-08-07：创建第一版 LeetCode 刷题档案，整理双指针、二分、二分答案、链表、栈和队列的已有进度与薄弱点。
- 2026-08-08：补充 946 验证栈序列和 950 按递增顺序显示卡牌，记录 `top` 下标语义、逆向模拟以及数组搬移边界问题。
- 2026-08-11：补充 2181 合并零之间的节点和 844 比较含退格的字符串，记录链表首轮特殊状态、自环风险、逆向双指针、退格计数器以及嵌套 while 的复杂度判断问题。
- 2026-08-12：补充 1700 无法吃午餐的学生数量和 725 分隔链表，记录队列模拟停止条件的 off-by-one、无效状态变量，以及链表切分和断链操作的稳定性。
- 2026-08-13：补充 622 设计循环队列和 2390 从字符串中移除星号；记录循环队列采用 `front == rear` 判空时需牺牲一个空槽，因此实际容量 `k` 需要内部数组长度 `k + 1`；同时确认字符串栈模拟已较稳定。
- 2026-08-15：补充 328 奇偶链表和 735 小行星碰撞；确认基础链表重连已经较稳定，后续链表题应提高状态复杂度；记录 735 中当前元素提前进入有效栈区导致 `top` 语义混乱，以及在高强度思考较久后明知需要重构但没有继续推翻原结构的问题。
- 2026-08-17：补充 143 重排链表；记录独立 O(n²) 重连方案可通过，但尾部前驱被反复线性扫描，且循环次数由数值匹配得到而非从不变量推导；后续需要重新独立完成 O(n) 的组合解法。
- 2026-08-19：补充 649 Dota2 参议院和 71 简化路径；记录 649 中双队列数组模拟已经能够独立完成，但 Java 标准队列 API 仍需补齐；记录 71 中因不了解 `String.split()` 而采用字符级路径解析，导致状态与边界迅速复杂化，并明确后续需要优先识别题目的自然处理单位（目录/token），而不是默认按字符建模。
- 2026-08-20：补充 82 删除排序链表中的重复元素 II 和 155 最小栈；记录 82 中能快速识别多指针方向，但 `pre` 职责、重复段结算与头尾边界仍不稳定；记录 155 中第一次接触辅助栈设计，关键双栈思路来自外部提示，随后能自行实现并调通，同时补充 O(1) 复杂度理解与双状态同步问题。
- 2026-08-21：补充 225 用队列实现栈；记录首次系统使用 Java Queue API，能够从无整体思路逐步推到辅助队列搬运并独立 AC；同时记录 `pop()` 中动态 `size()` 的循环边界问题，以及 `queue1 = queue2` 后两个引用合流却未继续推演，最终无意中形成单队列旋转这一状态推演问题。
- 2026-08-23：补充 92 反转链表 II；记录最初将 `left/right` 误读为节点值导致前期偏题，以及反转中修改 `fast.next` 后仍按旧链关系读取它造成成环的问题；确认局部反转主体已能独立完成，后续重点训练修改指针前保存旧关系。
- 2026-08-24：补充 1209 删除字符串中的所有相邻重复项 II；记录能想到需要额外计数但未能独立建立“字符数组与计数数组同下标绑定”的附加状态模型，以及完成主体后因两个独立 `if` 导致同一字符重复入栈的控制流问题；后续重点复习“值 + 状态”共同组成一个逻辑栈元素的建模方式。
- 2026-08-28：补充 856 括号的分数和 2 两数相加；记录 856 中首次明显遇到“栈保存层级计算状态而非实体”的建模困难，以及把当前层结算与父层累加误看成二选一操作的问题；记录 2 中哑节点、尾插、双链遍历和进位已经较熟练，同时提醒可用统一循环减少三段重复代码。
- 2026-08-31：复习 82 和 1209。82 已能主动使用哑节点，但仍因对原首节点写额外特判，并在 `continue` 前后没有同步推进 `second`、`third` 而出现状态错位；删除首节点特判后完成，说明哑节点统一头部边界的作用已经理解，但多状态同步仍需继续注意。1209 间隔一周后约十分钟独立完成，能够自然使用平行数组保存“字符 + 连续次数”这一逻辑栈元素，原先的附加状态建模问题已明显改善，当前可视为基本掌握。
- 2026-09-01：复习 206 反转链表和 155 最小栈。206 约十分钟不看提示独立完成，基础迭代反转和修改指针前保存旧关系已验证稳定，移出近期复习重点。155 能独立重新设计普通栈 + 最小栈并正确处理重复最小值与双栈同步；本次测试失败来自 Java 边界常量 `(int)Math.pow(2,31) - 1` 实际得到 `2147483646`，确认以后直接使用 `Integer.MAX_VALUE` / `Integer.MIN_VALUE`。